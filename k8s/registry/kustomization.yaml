namespace: infra

commonLabels:
  group: my-registry

resources:
  - "deployment.yaml"
  - "service.yaml"
  - "pvc.yaml"
  - "dind.yaml"
  # - "ingress.yaml"

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: scripts
    files:
      - "test.sh"