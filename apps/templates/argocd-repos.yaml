# Git repositories configure Argo CD with (optional).
# This list is updated when configuring/removing repos from the UI/CLI
# Note: the last example in the list would use a repository credential template, configured under "argocd-repo-creds.yaml".
apiVersion: v1
kind: Secret
metadata:
  name: gitea-dag-repo
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  url: {{ .Values.spec.gitea.server.outClusterURL }}/{{ .Values.spec.gitea.username }}/dag.git
  password: {{ .Values.spec.gitea.username }}
  username: {{ .Values.spec.gitea.password }}
---
apiVersion: v1
kind: Secret
metadata:
  name: drone-helm-repo
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  url: https://charts.drone.io
  name: drone
  type: helm